pub struct StringBuilder { # todo: ick!
    int capacity,
    int size,
    char** buf,
}

pub fn StringBuilder_into_int(StringBuilder* self) { # todo:jstdlib
    self.push(null);
    int i = atoi(self.buf);
    free(self.buf);
    free(self);
    return i;
}
