pub fn main() {
    puts("Hello, settlers_of_the_north_pole!");
    int line_count = 0;
    while !iseof() {
        free(read_line());
        line_count = line_count + 1;
    }
    printf("There are %d lines of input waiting!\n", line_count);
}
